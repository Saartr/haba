---
name: backlog
description: Функциональность, которую нужно реализовать позже (решение принято, реализация отложена); известные баги на исправление
metadata:
  type: project
---

## Известные баги (решение принято, чинить позже)

### Профиль недостижим из UI после редизайна главного экрана (2026-07-09)
Единственным входом на `/(tabs)/profile` (и через него `profile-settings`: имя, привязка
Telegram/VK, кнопка «Обновить аватар») был тап по аватару в старой шторке главного экрана.
Редизайн (см. [[feature-main-screen]]) её убрал; Toolbar ведёт только в `app-settings`, где
пункта «Профиль» нет. **Как чинить:** решить с пользователем — иконка в Toolbar, пункт в
app-settings или вариант из макета Figma (возможно, коллега доделает в следующей итерации).

### HabitCard на главном экране: карточки «Шаги» показывают стрик вместо «Шагов за сегодня»
**Файл:** `app/(tabs)/index.tsx`, функция `HabitCard` (тернарники `subtitle` и `value`, ~строки 103-120).

**Причина:** `checkin_type` в БД ограничен CHECK-constraint тремя значениями (`'boolean'|'count'|'progression'`). Ветки `isBoolean`/`isProgression`/`isCount` покрывают их все — значит финальная ветка `: 'Шагов за сегодня'` (и аналогичная в `value`: `` `${todayVal}/${habit.goal_value ?? 0}` ``) **никогда не выполняется**, это мёртвый код.

Пресеты категории «Шаги» создаются через `preset-habits.tsx`, который **не передаёт `checkin_type`** в `createHabit()` → бэкенд (`backend/src/api/habits.js:110`, `${checkin_type ?? 'boolean'}`) подставляет дефолт `'boolean'`. Из-за этого `isBoolean === true` для «Шагов», и подпись/значение карточки берутся из ветки для обычных boolean-целей: подпись «Текущий стрик» вместо «Шагов за сегодня», значение — голое число стрика вместо `2000/3000`.

При этом `done` (довольный/недовольный Тапа) в этой же ветке всё ещё сравнивает `todayVal` с `goal_value` — то есть аватар считается по шагам, а текст рядом показывает стрик. Несостыковка видна прямо на карточке.

**Как чинить:** добавить `habit.category === 'steps'` явной проверкой ПЕРЕД `isBoolean` в обеих ветках (`subtitle` и `value`) — по аналогии с тем, как уже приоритетно проверяются `smoking`/`pullups`.

**How to apply:** при следующей работе с `index.tsx`/`HabitCard` — исправить эту ветку, если пользователь попросит вернуться к багу.

### Fab.tsx: выпадающее меню уезжает за край экрана, если кнопка не в угловой обёртке
**Файл:** `components/Fab.tsx`.

Дропдаун-меню позиционируется как `position:'absolute', right:0, bottom: buttonSize+8` **относительно локального родителя**, не экрана. Работает корректно только когда `Fab` обёрнут в угловой `<View style={{position:'absolute', right:24, bottom:...}}>` (как на главном экране, `app/(tabs)/index.tsx`). Если `Fab` используется НЕ в угловой обёртке — например, внутри `components/Toolbar.tsx` или инлайн в `ScrollView` (галерея `app/dev.tsx`) — меню может уезжать за левый край экрана (ширина 360 при маленьком локальном контейнере) или быть обрезано `ScrollView`'ом.

**Что уже пробовали и почему откатили:** рендер меню через `Modal` (как у `DropdownPopover`) + измерение реальной позиции кнопки через `measureInWindow` — технически более правильный подход (не клипается `ScrollView`, подстраивается под положение на экране). Но `measureInWindow` в этом Dev Client/Expo-окружении стабильно возвращал координаты, НЕ соответствующие настоящей позиции кнопки на экране (подтверждено визуальной отладкой — красная линия-маркер на предполагаемой позиции кнопки рисовалась в другом месте, не у кнопки), независимо от того, что именно мерили (`Pressable` vs внутренний `View`) и в какой момент (сразу при открытии vs после `onLayout`/`onShow` модалки). Причина не выяснена. Отменено, возвращена простая версия без `Modal`.

**How to apply:** если понадобится нормально работающий `Fab`/`Toolbar` вне угловой позиции — исследовать `measureInWindow` отдельно (возможно, баг конкретно Dev Client, стоит проверить на чистом release-билде), либо использовать другой способ выхода за границы `ScrollView` (например, `DropdownPopover`-подобный фиксированный якорь вместо динамического измерения).

## Отложенные фичи (решения приняты)

### expo-clipboard — подключить при следующей пересборке APK
`Clipboard` из `react-native` deprecated. `expo-clipboard` установлен (`npm install` выполнен), но нативный модуль `ExpoClipboard` требует `prebuild`.
**Что нужно:** при следующем `npx expo prebuild --platform android --clean`:
1. В `app/(tabs)/habit/[id].tsx` заменить `import { Clipboard } from 'react-native'` → `import * as Clipboard from 'expo-clipboard'`
2. Заменить `Clipboard.setString(...)` → `Clipboard.setStringAsync(...)`
**Файл:** `app/(tabs)/habit/[id].tsx:14` и `:747` (номера строк сдвигаются по мере правок файла — искать по `import { Clipboard } from 'react-native'` и `Clipboard.setString`)

