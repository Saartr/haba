---
name: team
description: Кто есть кто — Михаил и Петр, у кого какие доступы (Play Console, Firebase, сервер, консоли VK/Яндекса, ключ подписи); память общая на обоих
metadata:
  type: user
---

Над «Тапой» работают двое, и **память в `.claude/memory/` у них общая** — её читают сессии Claude
у обоих. Поэтому людей в записях называть по именам, а не «пользователь» / «коллега»: в сессии
Петра «пользователь» — это Петр.

**Михаил** — в git `Saartr`, аккаунт Expo `saartr`. На машине Михаила:
- релизный ключ подписи `C:\Users\Saartr\tapa-release.jks`, пароли — в `~/.gradle/gradle.properties`
- SSH-доступ к серверу (`ssh Tapa`)
- консоли VK ID и Яндекс ID — через них настраивался вход, в том числе на iOS

**Петр** — в git `Mironovpeter10` (ветка `mironovpeter10/ui-components`). За Петром:
- публикация в Google Play: аккаунт разработчика и Play Console — у Петра ([[infra-google-play]])
- Firebase-проект `tapa-e67cf` — создан под Google-аккаунтом Петра ([[feature-push]])

**Как читать старые записи:** в записях до 2026-09-21 «пользователь» — это Михаил (все сессии
до этого шли с ним), «коллега» — Петр.

**How to apply:**
- в новых записях — имена: «решение Михаила: …», «в Play Console у Петра …»
- если задача упирается в доступ, которого у собеседника нет, сразу говорить, к кому идти:
  Firebase и Play Console — Петр; ключ подписи, сервер, консоли VK и Яндекса — Михаил
- ключ подписи на 2026-09-11 Петру не передавался; передавать только вне чата
